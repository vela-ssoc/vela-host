# host
> 主机信息获取

```lua
	print(vela.host.id)
	print(vela.host.name)
	print(vela.host.platform)
	print(vela.host.family)
	print(vela.host.version)
	print(vela.host.kernel)
	print(vela.host.uptime)
	print(vela.host.boot_time)
	print(vela.host.virtual)
	print(vela.host.virtual_role)
	print(vela.host.proc_number)
```